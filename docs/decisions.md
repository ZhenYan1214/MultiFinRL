# 規格決策紀錄（Decisions Log）

記錄已與負責人確認、或團隊內部定案的規格，避免口頭決議遺失。新決議往下追加，不刪除舊紀錄；若推翻舊決議，標註「已被 #N 取代」。

| # | 日期 | 決議 | 來源 |
|---|---|---|---|
| 1 | 2026-07 | 市場為美股；股票池以大型股/指數為準（AAPL、NASDAQ、QQQ） | 負責人 Q&A |
| 2 | 2026-07 | 指數/ETF 無財報與法說會，會影響資料格式 → **實作先以個股為主** | 負責人 Q&A |
| 3 | 2026-07 | 資料時間範圍以 5 年為準，儘可能長；論文可比較 3 年 / 5 年模擬績效 | 負責人 Q&A |
| 4 | 2026-07 | 訓練資料單位 = 股票 + 交易日；內容為圖片、新聞、法說會，可自行加 feature 做論文實驗 | 負責人 Q&A |
| 5 | 2026-07 | K 線圖先以 20 日為準，後續可加 5 日、60 日做 multi-scale 實驗 | 負責人 Q&A |
| 6 | 2026-07 | 當日無新聞以前幾日回補，加入「幾天前」（days_ago）feature 交給模型自行判斷相關性 | 負責人 Q&A |
| 7 | 2026-07 | 第一年需要完整可執行 pipeline，且需含**簡單回測**（單股比例增減或多標的組合皆可） | 負責人 Q&A |
| 8 | 2026-07 | 第一年原始驗證目標不可忘：**市場情緒分類與事件抽取的準確度** | 負責人 Q&A |
| 9 | 2026-07 | 股價來源候選 yfinance，是否合適與老師再確認 | 負責人 Q&A |
| 10 | 2026-07 | ViT 為自然圖片預訓練，與 K 線圖有 domain gap，需實測比較 | 負責人 Q&A |
| 11 | 2026-07 | 負責 QLoRA 者需一併做 RL，比較加入 RL 前後的績效/metric 變化 | 負責人 Q&A |
| 12 | 2026-07 | 開發環境以 repo 的 requirements.txt 為唯一依據 | 負責人 Q&A |
| 13 | 2026-07 | 圖片格式 PNG、224×224、RGB；Chunk ≤512 token；RAG K=3 | 負責人 Q&A |
| 14 | 2026-07 | 資料格式以負責人雲端範例檔（DataStruct.json / vectors.json）為原始依據，正式化為 docs/data_format.md | 負責人 Q&A |
| 15 | 2026-07 | 初始股票池先固定 AAPL 一檔，跑通後再擴充 | 團隊內部定案 |
| 16 | 2026-07 | 漲跌標籤精確定義：未來**第 5 個交易日收盤價 vs 當日收盤價**之報酬，> +2% → BULLISH，< −2% → BEARISH，其餘 NEUTRAL；ablation（3 天、±1% 等）留待論文實驗，第一版不變動 | 團隊內部定案 |
| 17 | 2026-07 | 新聞/法說會來源自行尋找，負責人無指定來源 | 負責人 Q&A |
| 18 | 2026-07 | **時程由兩年壓縮為一年**：原「第一年／第二年」內容全部併入同一年，改稱「第一階段／第二階段」；投資組合配置、完整回測、RL 不再是隔年才做，本年度即須完成（已被此決議取代前提下，README「兩年期目標」章節已改寫，內容不變只重新分期） | 負責人 Q&A |
| 19 | 2026-07 | RAG top-k 檢索的相似度計算/選取邏輯由 B 自行設計；唯一固定的是輸入為「當日」資料、輸出前 K 份最相關文件（K=3，見 #13） | 負責人 Q&A |
| 20 | 2026-07 | pipeline 必須整合投資組合配置與回測（不可只做到分類驗證就結束）；C 負責 QLoRA 者務必比較「有無 RL」的績效/metric 差異，此為必達項目而非選項（重申 #11，時程壓縮後仍不可省略） | 負責人 Q&A |
| 21 | 2026-07 | days_ago 的處理方式確認：與其他文字一起丟進 encoder，讓模型從輸入中自行學出「幾天前」對相關性強度的影響，不需額外人工規則 | 負責人 Q&A |
| 22 | 2026-07 | 技術選型不需堅持特定套件/模型（yfinance、ViT、FinBERT 等目前都只是預設值），只要效能更好、更好用就可以替換；資料/向量格式（data_format.md）不受此影響，換工具仍須維持格式不變 | 負責人 Q&A |
| 23 | 2026-07 | 盤後（收盤後）發布的新聞，歸屬規則確認為「前一天」；**目前解讀**：收盤後才發布的新聞視為隔一個交易日才「可得」，歸入下一個交易日的資料而非發布當天，避免當天資料出現實際上收盤後才知道的資訊——此為目前程式實作採用的解讀，若與老師原意不同需再確認調整 | 負責人 Q&A |
| 24 | 2026-07 | 分類驗證暫先沿用 #16 的標籤定義（未來第5個交易日 vs 當日收盤，±2% 三分類）；計畫書所寫「市場情緒」的正式定義（是否等同漲跌標籤、或需改用新聞文字情緒）仍待與老師進一步討論，非最終定案 | 負責人 Q&A |
| 25 | 2026-07 | 新聞來源可使用 FNSPID（歷史至2023）+ 自行爬取2024年後缺口；財報/法說會可使用外部開源工具（edgartools、foolcalls）；老師同意「先用看看是否符合需求」，非無條件背書，仍需team自行驗證品質 | 負責人 Q&A |
| 26 | 2026-08 | **#25 的 FNSPID 新聞回補實測結論：不可行，改用其他方法**。實測發現兩個獨立問題：(1) FNSPID 附帶的 headline/content scraper 對 nasdaq.com 執行時，自動化瀏覽器（Selenium 與 Claude in Chrome 兩種都測過）都卡在新聞列表載入骨架、抓不到真內容，但同一網址用一般手動瀏覽器可以正常看到，判斷是網站對自動化流量做偵測阻擋；且頁面前端已改版，scraper 寫死的 class 名稱（如 `quote-news-headlines__item`、`pagination__page--active`）在目前頁面上已不存在，需要重寫抓取邏輯，成功率沒有把握。(2) FNSPID 在 HuggingFace 上的現成 dataset，重新抽樣驗證後 `Article`（新聞全文）欄位仍全部為空，連 dataset 本身附的四種自動摘要欄位（Lsa/Luhn/Textrank/Lexrank_summary）也全部為空，判斷這批資料本來就沒有全文可用，非抽樣偶然。財報/法說會（edgartools、foolcalls）不受影響，維持可用。新聞歷史回補需要另尋資料來源/方法。 | 團隊內部定案（實測結果，docs/spec_a_news_backfill.md） |
| 27 | 2026-08 | **新聞歷史回補改用 Alpaca News API，實測成功，取代 #25/#26 的 FNSPID 方向**。改用官方新聞 API（Alpaca News API，來源 Benzinga，官方開放介面非爬蟲）取代 FNSPID；本機實測 AAPL 2021-01-01~2026-08-08，共抓到 15,989 則、涵蓋 1,459 天，總耗時約 10 分鐘（586 秒），無卡住/無自動化偵測阻擋問題，抽樣檢查 content 欄位皆為真實全文（非空、非佔位字串），週末新聞正確歸屬下一交易日（`_effective_date` 邏輯正常）。程式碼：`module_a_data/crawler/fetch_news_alpaca.py`。因此原本保留作參考的 FNSPID 相關程式碼（`FNSPID/` 目錄、`fetch_news_fnspid.py`、`fetch_news_local_scrape.py`）已實際刪除，`requirements.txt` 移除 selenium。 | 團隊內部定案（本機實測結果） |
| 28 | 2026-08 | **分類準確率診斷實驗結論：類別加權是關鍵，且能讓新聞資料的效果顯現出來**。#27 補齊五年新聞後，分類準確率幾乎沒變（0.459→0.465），懷疑類別不平衡（測試集 NEUTRAL/BULLISH/BEARISH 約 44%/35%/21%，訓練與分類皆未加權）才是真正瓶頸，設計「新聞有無 × 類別加權有無」2×2 對照實驗驗證（`docs/spec_c_accuracy_diagnostics.md`）。結果：不加權時有無新聞的分類結果逐位數字完全相同（模型實質沒學到用新聞）；加權後模型不再塌縮成只猜 NEUTRAL（BEARISH f1 從 0 提升到 0.196，macro f1 從 0.247 提升到 0.303），且加權狀態下「有新聞」在 accuracy、三個類別的 f1 上全面優於「沒新聞」——代表新聞資料確實有貢獻，先前只是被類別不平衡完全蓋住看不出來，兩者存在交互作用，不是各自獨立的問題。**決定**：類別加權（`fusion/train.py`、`classifier.py` 新增的 `--weighted` 選項）採納為之後訓練的預設做法；下一步依原訂優先順序，先延伸 AAPL 時間範圍至 2026-08（對齊新聞歷史涵蓋範圍）。誠實記錄：即使加權+有新聞，macro f1 也僅 0.303、BEARISH precision 僅 0.159，離堪用還有明顯差距，這一輪只是修掉一個方法論問題，不代表準確率已達標。 | 團隊內部定案（grill-me → to-spec → to-tickets → 實測結果，docs/spec_c_accuracy_diagnostics.md） |
| 29 | 2026-08 | **計畫盤點（grill-me）：對照正式計畫書（115WFAA310699_CM03）與自製分工 PDF，找出目前 A/B/C 尚未涵蓋或做得陽春的落差**。對照結果：(1) 正式計畫書 3.4 節的生成式 decoder（用 Z_fused 生成結構化市場敘述，訓練時另有 L_belief 損失）與 3.6 節的跨模態可解釋性（Integrated Gradients 套在 PPO policy 上）完全沒有實作，且自製分工 PDF、decisions.md、tickets 這些反映團隊實際工作範圍的文件原本都沒有涵蓋——**決定**：兩者確認屬於必要項目（decoder 屬第一階段 C、需 A 端另外產出結構化敘述標準答案，成本較高；可解釋性屬第二階段 C、可套用在已訓練好的 PPO policy 上，成本較低），但**先記錄、暫不動工，待與老師確認優先順序後再排入排程**。(2) 另外實際讀程式碼發現四項現有實作偏陽春或完全空白，且先前未被追蹤：事件抽取目前純粹是關鍵字比對（如偵測到 "earnings" 字樣即標記 EARNINGS），沒有語意理解，即使補上標準答案，抽取方法本身精準度仍待改善；`vision_encoder.py` 的 ViT 完全是凍結的預訓練特徵抽取器，`decisions.md` #10 要求的「ViT 與 K 線圖 domain gap 需實測比較」從未執行，遑論微調；`train_ppo.py` 沒有任何 curriculum learning 相關程式碼，是直接用全部歷史資料訓練，正式計畫書列為 RL 訓練穩定性風險應對方案之一，目前未實作；`backtest.py` 的 `run_backtest` 只吃一維 `weights`（單一股票的持股比例），目前僅支援單一股票（AAPL）的持有/空手配置，尚未真正支援多資產（多支股票）投資組合配置，`decisions.md` #1/#15 講的「先固定 AAPL、跑通後再擴充」的「擴充」還沒開始。這四項連同既有的事件抽取標準答案缺口、RAG query 平均 vs 加權、FAISS K 值未調參，一併記入下方待確認事項表。 | 團隊內部定案（grill-me 討論結果） |
| 30 | 2026-08 | **漲跌標籤定義改為分位數門檻，取代 #16 的固定 ±2%**。動機：#28 診斷實驗修完類別加權、新聞、RAG、日期範圍後，macro f1 始終卡在 0.25~0.30，沒有明顯突破，符合 `spec_c_accuracy_diagnostics.md` 當初預留的判斷標準（資料/訓練面修完仍無起色，下一步檢討標籤定義）。實測發現固定 ±2% 門檻有具體問題：AAPL 報酬標準差 3.88%，2% 門檻離 0 不到一個標準差，切在分布最密集處；有 12.1% 的天數報酬落在門檻正負 0.3 個百分點以內，這些「邊界模糊天」的分類錯誤未必反映模型真的判斷失誤。查了同類論文（stock movement / FinBERT 相關文獻），固定門檻與分位數門檻皆為常見做法，分位數門檻常被用來降低雜訊、讓類別自然平衡。**改法**：`module_a_data/labeling.py` 新增 `compute_quantile_thresholds()`，用整段期間（該 ticker 全部日期）的未來 5 日報酬分布，取 1/3、2/3 分位數當 BEARISH/BULLISH 門檻，`build_dataset.py` 只算一次、每天共用同一組門檻（不是每天重算，避免每日門檻不一致）。舊版固定 ±2% 邏輯保留在 `make_label_fixed_threshold()`，供之後想做門檻定義 ablation 時對照。實測 AAPL 算出的門檻：bearish<−1.10%、bullish>+1.97%，三類天數變成 33.4%/33.3%/33.4%，接近完美平衡。**已知取捨**：門檻是用整段期間（含未來）的報酬分布統計特性算出來的固定值，跟原本人為選定 2% 的性質相同（都是資料設計者定規則時的判斷），不是模型訓練當下看到未來資訊，但這個假設本身值得记录、之後如有疑慮可重新檢視。**實測結果（2026-08-10）**：重跑 `build_dataset.py` → `fusion.train` → `classifier.py`（向量不受標籤影響，不需重跑 `generate_vectors.py`），在跟前一輪完全相同的 966/207/208 切分與測試期間下做乾淨對照：macro f1 從 0.286 提升到 0.324，accuracy 從 0.303 提升到 0.327，BEARISH/BULLISH f1 都有進步，測試集類別分布也從明顯不平均變成三類接近平均（support 65/67/76）。這是這一路診斷以來第一次無混雜因素的正向結果，詳見 `docs/data_and_experiments_log.md` 第二節 2026-08-10 那一行。此決議取代 #16。 | 團隊內部定案（累積診斷結果 + 文獻研究 + 實測） |
| 31 | 2026-08 | **財報抓取維持 `fetch_filings.py`（原生 requests 版）現役，`fetch_filings_edgartools.py` 保留為候選方案，不刪除**。查磁碟上 `data/raw/filings/AAPL/` 現有檔案皆為 `.html`（`fetch_filings.py` 的輸出格式；`fetch_filings_edgartools.py` 輸出 `.txt`），確認目前資料實際由 `fetch_filings.py` 產出，edgartools 版本從未真正產出過現有資料。edgartools 理論上可能有更乾淨的文字擷取、且內建結構化財務數字（XBRL）擷取能力，是合理的未來候選，但目前累積的診斷證據（#28 類別加權、#30 標籤定義）都指向別處，沒有證據顯示 filing 文字品質是現在的瓶頸，暫不投入時間做 A/B 比較。**決定**：兩支都保留在 repo，`fetch_filings.py` 為現役，`fetch_filings_edgartools.py` 留待之後若懷疑 filing 資料品質有問題時再拿來做對照實驗。同批順便完成專案結構整理：`.claude/`（Claude/Cowork session 本機設定）加入 `.gitignore`；原本放在根目錄的執行順序小抄 `conductScript.md` 搬到 `docs/conduct_script.md`，跟其他文件放一起，並更新內容對齊目前狀態（新聞改用 Alpaca API、標籤改用分位數門檻、C 訓練預設帶 `--weighted`）。 | 團隊內部定案（grill-me 討論結果 + 實際檔案格式比對） |
| 32 | 2026-08 | **事件抽取補上 ground truth，量出關鍵字方法的真實 P/R/F1，並修掉找到的方法論問題**。經 grill-me 討論定案做法：ground truth 用 LLM 輔助（這一輪由 Claude 在對話中直接標記，未來個股數量變多時可切換成呼叫 API 的腳本，兩者共用同一份 prompt，見 `module_b_encoder/event_ground_truth_prompt.py`），採 150 天（實際 149 天，四捨五入誤差）分層隨機抽樣（依年份比例抽樣，`stratified_sample()`），分 15 批、每批約 10 天，用 subagent 平行處理，存到 `data/labels/event_ground_truth/AAPL.json`（進版本控制，答案卷弄丟需要重標）。**Baseline 結果**（改進前）：precision=0.073、recall=0.987、f1=0.135——幾乎每天每一類都被標記，嚴重誤判。**根因排查發現，公司相關性不是主因**：先加上「新聞標題要出現公司名稱/ticker 才算」的相關性檢查，precision 只從 0.073 微升到 0.078（fp 959→886），效果很小。追查發現真正主因是 `filing_chunks`/`transcript_chunks`——`build_dataset.py` 的 `collect_filing_chunks()`/`collect_transcript_chunks()` 設計成「沿用最新一份財報/法說會到下一份發布為止」，同一份文件的樣板文字（股利政策段落、訴訟揭露段落、併購風險因素段落等）會被連續很多天重複讀到並誤判成「當天發生」的事件，事件抽取程式原本沒有檢查 chunk 的 `filing_date`/`event_date` 是否等於當天日期。**改法**：`event_extraction.py` 新增日期比對，只有 chunk 的 `filing_date`/`event_date` 等於當天才進關鍵字比對，否則視為背景脈絡略過；同時修正過於寬鬆的關鍵字（`PRODUCT_LAUNCH` 拿掉 "announce"/"release"、`GUIDANCE` 的單字 "raise"/"lower"/"cut" 改成 "raised its guidance" 等完整片語、`DIVIDEND` 的 "split" 改成 "stock split"、`MANAGEMENT_CHANGE` 拿掉單獨的 "ceo"/"cfo"，只保留真正指向異動的詞）。**結果**：precision 0.073→0.162、recall 0.987→0.868、f1 0.135→0.273（相對提升約 102%），fp 從 959 降到 341。`evaluate()` 正式接進 `main()`，`event_extraction_report.json` 從此輸出真實 P/R/F1，不再是 "pending ground truth"。**誠實記錄目前限制**：precision 絕對值仍偏低（0.162），`EARNINGS`/`GUIDANCE`/`PRODUCT_LAUNCH` 仍有不少誤判（純關鍵字比對難以分辨「討論某件事」跟「當天真的宣布」的差別）；ground truth 裡唯一一筆 `MANAGEMENT_CHANGE`（用非正式措辭報導的人事异動）目前仍抓不到，反映純關鍵字方法的天花板，這也是這支程式檔頭本來就寫的「後續可改成 LLM-based 抽取」的動機來源，非這一輪範圍。順便把呼叫 LLM API 建 ground truth 的腳本架構寫好（`module_b_encoder/event_ground_truth_llm.py`，支援 `--provider claude/openai`），這次沒有執行，留給之後個股數量變多、要擴大標記範圍時直接啟用。 | 團隊內部定案（grill-me → 實測結果） |

| 33 | 2026-08 | **釐清事件抽取的定位、跟正式計畫書/自製分工 PDF 原文核對、決定改用 LLM-based 抽取方法**。重新讀正式計畫書全文（`115WFAA310699_CM03`）確認：「event extraction」全文只出現一次，在 2.3 節文獻回顧，是描述 BloombergGPT/FinGPT 等其他財經 LLM 的能力當研究背景，**不是**這個計畫自己方法論章節（3.4-3.6 節）規定的工作項目，計畫書從未指定事件抽取要用什麼方法。自製分工 PDF 第 6 頁原文寫「驗證 Z_fused 的準確率（市場情緒分類與事件抽取）」，代表原始設計意圖是把事件抽取當成跟分類驗證並列、驗證 Z_fused 品質的兩個探測方法之一。**發現落差**：`event_extraction.py` 目前完全不讀 Z_fused，只讀 A 的原始 JSON，等於只驗證了「A 收集的原始資料裡有沒有真事件的痕跡」，沒有驗證「Z_fused 這個融合向量本身有沒有捕捉到這些事件」，跟原始設計意圖有落差。**決定**：(1) 事件抽取的抽取方法改用 LLM-based（取代目前的關鍵字規則），`extract_events()` 回傳格式不變，之後直接替換內部實作即可，不影響其他程式碼；(2) 記錄一個新的待做項目：幫 Z_fused 加一個「事件驗證頭」（`Z_fused → 線性層 → 預測當天事件類型（multi-label）`，跟現有分類驗證頭平行，用同一批 `data/labels/event_ground_truth/` 當答案比對算 P/R/F1），這才是真正落實原始設計意圖「用事件抽取驗證 Z_fused」的做法，目前完全沒有實作；(3) 目前已存在的 `event_extraction.py`（讀 A 原始資料，不碰 Z_fused）繼續保留，角色調整為「A 資料品質下限檢查」，跟新的事件驗證頭是互補關係，不是要被取代。順便釐清一般文獻/業界事件抽取的定位：通常是拿抽出來的事件本身直接當訊號用（交易訊號、知識庫、下游模型的特徵），不是主要拿來檢查別的資料好不好——用它來驗證 Z_fused，是這個計畫自己的設計選擇，不是業界通用做法。 | 使用者釐清討論 + 重新核對正式計畫書與分工 PDF 原文 |
| 34 | 2026-08 | **實作事件驗證頭，補上 #33 提出的落差，第一次真正驗證 Z_fused 有沒有吸收事件資訊**。`module_c_fusion/validation/event_validation_head.py`：拿 149 天 ground truth 對應的 Z_fused（768 維）當輸入，`LogisticRegression`（`class_weight="balanced"`）逐類別訓練 multi-label 分類器，用 5-fold cross-validation（而非切一份固定 train/test，樣本數只有 149 天，切了會不穩定）評估 P/R/F1，不需要 torch，可以直接在 sandbox 跑。`MA`、`MANAGEMENT_CHANGE` 兩類在 149 天裡各只出現 1 次，正樣本太少 cross-validation 無意義，程式明確標注「資料量不足，不評估」而非硬跑出不可信的數字。**結果**：可評估的 5 類（EARNINGS/PRODUCT_LAUNCH/LAWSUIT/GUIDANCE/DIVIDEND）micro-avg precision=0.147、recall=0.514、f1=0.229，跟事件抽取直接讀文字的 f1=0.273（#32）同一個量級，代表 Z_fused 確實吸收了一定程度的事件相關資訊，沒有在融合過程中把這類資訊完全丟失，是目前為止能回答「Z_fused 有沒有用」這個核心問題最直接的一筆證據。**誠實記錄限制**：樣本數小（149 天）、768 維輸入，屬於高維度低樣本的困難設定，且各類別正樣本數落差極大（LAWSUIT 36 天 vs 部分類別個位數），數字噪音大，這是初步結果、不是定論；`class_weight="balanced"` 會讓recall偏高、precision偏低，跟事件抽取（未用平衡權重）的數字不是完全同條件的對照，解讀時要留意。 | 使用者確認保留事件抽取與事件驗證頭 + 實測結果 |
| 35 | 2026-08 | **實作事件抽取的 LLM-based 方法，落實 #33 的決定；grill-me 定案設計、寫成 spec（`docs/spec_b_event_extraction_llm.md`），149 天樣本已在使用者本機實際執行，結果大幅優於關鍵字方法**。新增 `module_b_encoder/llm_client.py`：共用的 LLM 呼叫層，支援 `claude`/`openai`/`deepseek` 三種服務商（`deepseek` 走 OpenAI 相容介面，用 `openai` 套件換 `base_url` 打過去，不用額外裝 SDK），`label_events()` 統一組 prompt、呼叫、解析結果；`event_ground_truth_llm.py` 重構成只保留分層抽樣邏輯，呼叫改用共用模組，避免兩邊各自維護一份呼叫程式碼。`event_extraction.py` 新增 `--method {keyword,llm}`：`llm` 方法一天一次呼叫（整合當天新聞/財報/法說會，跟 ground truth 標記粒度一致，換取來源追蹤細節），送出前用既有的公司相關性檢查過濾新聞、用日期比對過濾沿用中的舊財報/法說會 chunk（跟關鍵字方法同一套過濾邏輯，避免重蹈 #32 抓到的「舊資料被誤判成新事件」問題），支援中斷續跑（結果邊跑邊存進 `data/outputs/metrics/event_extraction_llm_cache/{ticker}.json`，重跑跳過已有結果的日期），輸出到獨立的 `event_extraction_report_llm_{provider}.json`，不覆蓋關鍵字方法的既有報告。新增 `--limit_to_ground_truth`，可以只在 149 天 ground truth 樣本上跑，先驗證效果再決定要不要花全額成本跑全量 1381 天。**驗證方式**：沙盒環境沒有安裝 `openai`/`anthropic` 套件、無法實際打 API，先用 `py_compile` 確認語法正確，並對三個純邏輯部分寫了不打真實 API 的單元測試——`parse_events()` 解析各種格式的回應、`_relevant_filing_transcript_chunks()` 的日期過濾、`extract_llm_from_daily_record()` 送進 LLM 前的組裝內容、`run_llm_extraction()` 的中斷續跑跳過邏輯——全數通過。實測發現沙盒的網路可以連到 `api.anthropic.com`（回應 401，即連線成功只是沒帶金鑰），但連不到 `api.deepseek.com`（DNS 解析失敗），所以指定用 DeepSeek 實際執行必須在使用者本機、有一般網路連線的環境進行，不能在沙盒裡跑；過程中修掉一個真實 bug——`llm_client.py` 原本沒有載入 `.env`，導致 `DEEPSEEK_API_KEY` 寫在檔案裡卻讀不到，補上跟 `fetch_news_alpaca.py` 同樣的 `python-dotenv` 載入方式後修復。**149 天實測結果（`--provider deepseek --limit_to_ground_truth`）**：precision=0.742、recall=0.605、f1=0.667，對照關鍵字方法的 precision=0.162、recall=0.868、f1=0.273（#32），f1 提升約 144%。逐類別拆解（ground truth 次數 → LLM 抽到次數）：LAWSUIT 36→21、PRODUCT_LAUNCH 15→11、EARNINGS 10→9、GUIDANCE 7→9、DIVIDEND 6→8、MANAGEMENT_CHANGE 1→4、MA 1→0。**解讀**：precision 大幅提升，代表 LLM 判斷比關鍵字準確非常多；但 recall 下降，LLM 比關鍵字保守、會漏掉一些真的發生的事件，其中 LAWSUIT 漏最多（漏 15/36）、MA 唯一一筆完全沒抓到——這是「關鍵字寧可錯殺不放過 vs. LLM 判斷更準但較不敢下判斷」的預期取捨，不是程式錯誤。**決定**：確認效果後，接著在使用者本機跑全量 1381 天（拿掉 `--limit_to_ground_truth`，149 天已跑過的結果留在快取裡會自動跳過，只對剩餘天數計費）。**全量 1381 天實測結果**：1381 天全部處理完成，其中 401 天被判斷有發生事件，事件分布 LAWSUIT 218、PRODUCT_LAUNCH 129、MANAGEMENT_CHANGE 49、EARNINGS 43、GUIDANCE 38、DIVIDEND 34、MA 10；P/R/F1 跟 149 天測試時完全相同（precision=0.742、recall=0.605、f1=0.667）——這是預期行為，`evaluate()` 一律只對照 149 天 ground truth 涵蓋的日期計算，不受總處理天數影響，全量跑的意義是把完整 1381 天的事件抽取結果都準備好（`event_extraction_report_llm_deepseek.json`），不是讓分數改變。至此 LLM-based 事件抽取方法從設計到全量執行全部完成。 | 使用者確認 grill-me 決議（DeepSeek 優先、可切換服務商、149 天先測、其餘設計見 spec）+ 實作 + 單元測試 + 149 天實測結果 + 確認跑全量 + 全量 1381 天實測結果 |

| 36 | 2026-08 | **拿到正式計畫書 PDF 原檔，存進 `docs/reference/` 永久保存；重新核對 3.1 節 Vision encoder 原文，修正先前對 ViT 來源的推論**。原檔（負責人提供）連同純文字擷取版存進 `docs/reference/`（`115WFAA310699_CM03.pdf`、`_extracted_text.txt`），之後要核對計畫書原文不用再靠記憶或轉述，直接查這裡。**核對結果**：計畫書第 9 頁、3.1 節 Vision encoder 原文寫「passed through a vision encoder 𝑔(⋅) (e.g., CLIP or ViT pretrained on financial chart corpora)」——計畫書確實有提到 ViT，但明確限定是「用財經圖表語料預訓練過」的版本，不是現在 `vision_encoder.py` 實際在用的通用自然圖片預訓練版本（`google/vit-base-patch16-224`，ImageNet 預訓練）。這修正了先前的推論：#10 記錄的 domain gap 疑慮，不只是負責人口頭提出的疑慮，是目前實作跟計畫書 3.1 節原文規格之間真實存在的落差——計畫書要的是財經圖表領域預訓練的視覺編碼器，現在用的是完全沒做過領域調整的通用版本。 | 使用者提供計畫書原檔 + 重新核對原文 |

| 37 | 2026-08 | **修正 #33：計畫書其實有明訂「事件驗證頭」在做的事，只是用中文寫、之前搜尋沒找到；同時釐清這跟 `event_extraction.py` 是兩件不同的事，只是撞名**。#33 當時搜尋計畫書全文的「event extraction」（英文），只找到 2.3 節文獻回顧那一次背景描述，因此下結論「計畫書沒有指定事件抽取要用什麼方法、也沒有要求做這件事」，這個結論不完整——搜尋方式漏掉了計畫書裡用中文寫的段落。這次重新核對，在第 19 頁、section (三)「預期完成之工作項目及成果」（正式、逐年列出的必達工作項目，不是文獻回顧背景）找到：「表徵效能驗證：針對 𝑍𝑓𝑢𝑠𝑒𝑑 進行診斷性分類測試（Diagnostic Classification），驗證其在市場情緒分類與事件抽取上的準確度。」**這段話明確要求：拿 Z_fused 當輸入做診斷性分類測試（probing/diagnostic classifier，跟 `event_validation_head.py` 用的技術完全相同），驗證的對象是 Z_fused 本身在事件判斷上準不準**——這正是 `event_validation_head.py`（#34）在做的事，不是 `event_extraction.py`（讀 A 原始資料，不碰 Z_fused）在做的事。**修正**：(1) #34 事件驗證頭的定位要修正——先前記錄為「這次由使用者確認保留、我方提出的新設計，兩份 PDF 都沒有」，這個說法不準確，它其實直接對應計畫書第 19 頁明訂的正式工作項目，正當性比原先認知的更強；(2) 計畫書這裡用的「事件抽取」一詞，指的是「對 Z_fused 做診斷分類」這件事，跟程式碼裡命名為 `event_extraction.py` 的那支「讀 A 原始資料、判斷當天有沒有事件」的程式，是兩個不同的東西，只是剛好共用了「事件抽取」這個名字，造成先前討論時的混淆；(3) `event_extraction.py` 本身的定位不變，維持沒有計畫書依據、只能定位成 Track A 資料集品質驗證的附屬分析（149 天測試已足夠支撐這個定位，不需要再投入資源，見下方待確認事項表的修正）。 | 使用者要求核對「計畫書有沒有提到事件驗證頭」+ 重新搜尋計畫書中文段落 |

| 38 | 2026-08 | **教授確認兩個問題：市場情緒分類的標籤定義、ETF 的資料來源**。使用者直接問教授兩題：(1) 目前用未來 5 個交易日漲跌幅當「市場情緒」標籤，是不是計畫書要的定義；(2) 計畫的標的包含 ETF（如 QQQ），ETF 沒有財報、法說會，要不要改用總經資料。**教授回覆 (1)**：「市場情緒等於是一個指標，也是我們多模態融合出來的結果。true label 可以用未來五個交易日的漲跌來分類，但實際上可能會需要更多的技術指標來輔助，例如成交量、交易頻率、事件觸發等」——確認目前用未來 5 日漲跌幅當標籤的做法方向正確（是「價格走勢反推」的定義，不是「新聞文字語氣情緒」），但建議標籤定義之後可以加入更多輔助訊號（成交量、交易頻率、事件觸發），不是只看單一價格報酬率；教授額外補充「當然也可以先做 baseline，確保有結果後再擴展」，代表現在不需要立刻改動，先以現有做法為 baseline，之後有餘力再擴充。**這裡的「事件觸發」，是事件資料除了驗證 Z_fused（#37）之外，第一次出現的另一個潛在用途——拿事件發生與否，當作輔助定義漲跌標籤更可靠的訊號之一，不是驗證 Z_fused，而是幫助定義 A 的 label。這個標籤定義的擴充屬於 A（`labeling.py`）的範圍，不需要更動 B/C 的模型架構，只是改變「答案」怎麼算，暫不動工，待老師進一步確認或之後有餘力時再做。**教授回覆 (2)**：「可以，原本就是會包含 PCE、CPI 等宏觀數據，甚至你可以考慮美聯儲的點陣圖也符合我們多模態的 model」——確認 ETF 改用總經資料的方向正確，補充 PCE（個人消費支出）當額外候選指標；額外提議聯準會利率點陣圖（FOMC 點陣圖），因為點陣圖本身是圖表格式，可以直接沿用現有的 ViT 視覺編碼器處理，跟 K 線圖用同一套架構，不需要另外設計新的視覺輸入格式，是計畫書多模態設計裡一個具體、可行的延伸方向。**後續處理**：新增 `module_a_data/crawler/fetch_macro.py` 骨架（三個函式：總經指標、聯準會點陣圖、總經新聞，皆為 `NotImplementedError`，只寫架構不實作），對應新增 `shared/paths.RAW_MACRO`，待之後真的要擴充到 ETF/指數時再回來補上實作、確認資料來源（總經指標候選來源：FRED API）。目前計畫仍以個股（AAPL）為主，這支腳本不影響現有 pipeline。 | 使用者直接詢問教授 + 教授書面回覆 |

| 39 | 2026-08 | **負責人提出新提案：三版本 Z_fused 消融實驗（H_e 當第四個輸入），已確認 PDF 依據並釐清跟現有機制的關係，尚未定案，待負責人回覆**。負責人看到 `event_extraction.py` 149 天/1381 天實測結果（#35）後回覆：「你可以做成三個版本的：1.無事件抽取的 Z_fused 2.關鍵字判斷的 3. LLM-based 判斷的，剛好可以寫進論文，做相關性分析；再來是應該要利用 Z_fused 跟最終的判讀模型(計劃書內是寫 LLaVA / LLaMA-2)進行最終的預測」。**釐清架構**：三版本是把事件抽取結果（keyword/llm）當作新的第四個輸入 H_e，`Z_fused = F([H_t;H_v;H_r])` 改成 `Z_fused = F([H_t;H_v;H_r;H_e])`，H_t/H_v/H_r 三者不變，只換 H_e（無/關鍵字/LLM），跑三次融合各自產生一個 Z_fused 再比較，方向是「事件→產生 Z_fused」。**這是負責人自己提出的論文方法論加強，不是計畫書要求的**——全文搜尋「correlation」「ablation」都沒有這個三版本比較的設計，PDF 裡跟事件抽取有關的唯一正式要求仍是 #37 找到的 page 19 診斷分類測試（事件驗證頭在做的），方向相反（Z_fused→預測事件）。**LLaVA/LLaMA-2 部分屬實**：查證 PDF 3.4 節「Cross-Modal Fusion and Decoder」（page 12）原文「the decoder D(·), instantiated as a causal language model (e.g., LLaVA or a domain-adapted LLaMA-2), autoregressively generates outputs Y...conditioned on the fused representation」，對照 page 7-8「Phase 1 focuses on perception and belief construction, generating Z_fused」的分期敘述，確認 3.4 節屬於**第一年/Phase 1**（不是第二年/Phase 2；第二年 page 13-14 描述的是 PPO 策略訓練，全文沒有再出現 LLaVA/LLaMA-2 或 decoder 字眼）。decoder 本身目前完全未實作，是 #29 已記錄、待老師確認優先順序的既有缺口，這次沒有新增工作，只是確認負責人講的 decoder 確實有 PDF 依據、屬於第一階段。**待負責人回覆才能定案的疑慮**：(1) 三版本要比較的「準確度」沒講清楚是指 Z_fused 預測市場情緒（用 A 的價格標籤，跟事件無關）、還是 Z_fused 預測事件本身（套用事件驗證頭的技術，對照 149 天 ground truth）——已回問負責人待答；(2) 若是後者，149 天 ground truth 本身是 LLM（Claude）標記的（見 #32 原文「LLM 輔助標記，這一輪由 Claude 在對話中直接標記」），拿 LLM-based 方法（DeepSeek）去對比 LLM 標記的答案，量出來的準確度主要反映「兩個 LLM 彼此同不同意」，不是對客觀事實的準確度；這個 circularity 問題只有選(1)的「事件準確度」時才成立，選市場情緒準確度不受影響，已一併回問負責人；(3) 負責人誤以為事件抽取原本規劃是 RAG 要做的 H_r 向量之一，已查證 PDF 3.3 節（page 10-11）H_r 定義純粹是相似度檢索出的文件、跟事件抽取無關，現有 `retriever.py` 程式碼也是同樣做法，已回覆負責人釐清；負責人表示隔天會自己對照 PDF 跟 GitHub 再回覆，此點待他回覆後才能確定要不要調整規劃。**目前狀態**：以上皆為進行中的討論，H_e 三版本消融實驗尚未開始寫程式，等負責人回覆前述疑慮後再決定是否動工、怎麼動工。 | 負責人提案，使用者釐清疑慮並回問，待負責人回覆定案 |

| 40 | 2026-08 | **完整核對計畫書全文含圖片，確認先前純文字擷取沒有漏看內容；新發現一個較輕微的落差（Technical Indicators 應獨立視覺化）**。用 `pypdf` 掃過整份 PDF 全部 24 頁，確認全篇只有 1 張內嵌圖片（page 8，Figure 1「Multimodal-RAG Financial Decision Framework」架構圖），已直接抽出圖片檔案查看內容；其餘 Table 1、Table 2、page 20-21 甘特圖皆為文字/區塊字元排版，先前的純文字擷取版本（`docs/reference/115WFAA310699_CM03_extracted_text.txt`）已完整涵蓋，確認沒有遺漏。**Figure 1 內容跟正文架構描述一致**（Year 1 感知/信念建構、Year 2 決策優化、底部橫跨兩年的 Interpretability Backbone 分成 Modality Attribution 與 Policy Attribution），沒有發現跟正文矛盾之處。**新發現的細節**：Figure 1 把 Visual Inputs 明確畫成兩個獨立方框——「Candlestick Charts」與「Technical Indicators」分開，暗示技術指標本身可能也要單獨做視覺化輸入，不是只有 K 線圖一張圖；正文 3.1 節只舉 candlestick charts 為例，沒有像圖裡這樣明確拆開，先前的文字比對沒有抓到這個圖示細節。目前 `chart_generator.py` 只產生 K 線圖 PNG，沒有另外做技術指標的視覺化。**這是程度較輕、新發現的落差，是否要補上待團隊評估，非緊急**，先記錄，不影響目前已有的實作。 | 使用者要求核對「PDF 是否包含圖片內容都讀過」+ 重新用 pypdf 掃描確認 |

| 41 | 2026-08 | **`fetch_filings_edgartools.py` 候選方案確認刪除，取代 #31**。#31 原本決定兩支財報抓取程式並存（`fetch_filings.py` 現役、`fetch_filings_edgartools.py` 留待日後懷疑資料品質時再拿來對照）。這次使用者重新盤點 Track A 財報來源時，確認不需要保留候選方案，直接刪除 `fetch_filings_edgartools.py`，一併移除 `requirements.txt` 的 `edgartools` 依賴、`docs/conduct_script.md` 與 `docs/data_and_experiments_log.md` 裡對它的引用。`fetch_filings.py`（SEC EDGAR 官方 API + 原生 requests）維持唯一版本，不受影響。**同一輪盤點順便發現一個先前未記錄的落差**：`fetch_filings.py` 的 `FORMS = {"10-K", "10-Q"}` 只抓年報／季報，沒有抓 8-K（重大訊息即時揭露表）。EARNINGS／GUIDANCE／DIVIDEND／MANAGEMENT_CHANGE 這類事件在美股慣例上多半是透過 8-K 即時揭露，不是等到下一份 10-Q/10-K 才寫進去；目前的 `filing_chunks` 輸入結構性地抓不到這類即時事件，只能等隔一季後才在季報/年報裡看到滯後版本。這跟 #32 排查出的問題（同一份財報被沿用整季、樣板文字被誤判成新事件）同一個根源——因為抓的本來就是低頻更新的文件類型。是否要新增 8-K 抓取，記入下方待確認事項表，暫不動工。 | 使用者盤點 Track A 財報來源 + 重新讀程式碼確認 |

| 42 | 2026-08 | **三版本 Z_fused 消融實驗（#39 提案）確認不做，取代 #39 的待確認狀態**。使用者重新跟負責人釐清「事件抽取」在計畫書裡的定位：對照第19頁原文（「表徵效能驗證：針對 Z_fused 進行診斷性分類測試，驗證其在市場情緒分類與事件抽取上的準確度」）與張教授的說明後，確認這句話講的「事件抽取準確度」，指的正是既有的事件驗證頭（`event_validation_head.py`，Z_fused 回推事件類別）跟分類驗證（`classifier.py`，Z_fused 回推市場情緒），兩者合起來就是張教授所講「一系列的診斷性分類測試」，計畫書要求的驗證項目已經被這兩支既有程式滿足。#39 提三版本消融實驗的原始動機——讓獨立於 Z_fused 之外的 `event_extraction.py`（讀 A 原始資料算 F1，不碰 Z_fused）跟系統核心產生關聯——因此不再成立，這個關聯已經由事件驗證頭達成，不需要額外把事件抽取結果做成第四輸入 H_e。負責人確認「先不用做」。`event_extraction.py` 維持原本定位（Track A 資料品質檢查的附屬分析，非系統模組，見 #35/#37），不再需要為了三版本實驗投入額外工程（修改 `fusion/model.py` 架構、設計事件標籤編碼方式等）。**副作用**：先前 #39 提出的三個待確認疑慮（比較準確度的定義、LLM ground truth 的 circularity、H_r 誤解）全部一併失去討論意義，不需要再單獨解決。 | 使用者與王崇穎、張教授討論確認 |

| 43 | 2026-08 | **架構圖大改：分類驗證、事件驗證頭、Decoder 三者在概念上合併為一個元件；獨立的「事件抽取」節點從 Z_fused 主線架構圖移除**。承接 #42 的結論，王崇穎進一步提出：Decoder（計畫書 3.4/3.5 節，LLaVA/LLaMA-2，生成結構化市場敘述，訓練時 belief token 如 `[BULLISH]`/`[HIGH_VOLATILITY]` 本身就在做市場情緒判斷）跟現有的 `classifier.py`（市場情緒分類）在**功能意圖上是同一件事**，只是 Decoder 用生成式做、`classifier.py` 用獨立探測分類器做。查證屬實：`fusion/train.py` 檔頭本來就寫「基礎版：以情緒分類為訓練目標（cross-entropy）...QLoRA / 對比損失（L_align+L_ground+L_belief）為後續強化」，`classifier.py`／`event_validation_head.py` 一直都是 Decoder+L_belief 這整套機制**尚未做出來之前的簡化代打版本**，不是三個各自獨立設計的東西。**決定**：(1) 架構圖上把「分類驗證」「事件驗證頭」「Decoder」三個框合併成一個（暫定名稱「Z_fused 的 Classify 與 Decoder」，內容維持列出三支各自的腳本名稱與職責，不寫成敘述文字，因為這是描述專案架構的圖，不是進度報告）；(2) 原本獨立畫在 Module A 右側、代表 `event_extraction.py` 的「事件抽取」節點跟「事件抽取結果」節點，從這張圖整個移除——**只是移除架構圖上的視覺呈現，不是刪除程式碼或文件**，`event_extraction.py` 本身、它的測試結果（f1=0.273/0.667）、以及 `decisions.md`／`module_b_encoder/README.md`／`spec_b_event_extraction_llm.md` 裡對它的既有記錄全部保留不動，維持原本定位（Track A 資料品質檢查附屬分析，見 #35/#37），只是這張圖不再呈現它，因為它本來就不屬於 Z_fused 的核心產出路徑。**目前狀態**：這一輪**只是架構圖與觀念上的釐清、重新分類**，Decoder 本身依然完全未實作（#29 記錄的既有缺口不變，仍待老師確認優先順序），`classifier.py`／`event_validation_head.py` 兩支程式的程式碼、輸出、彼此獨立訓練/預測不同目標（市場情緒 3 類 vs 事件類型 7 類多標籤）的事實都沒有改變，**不能因為架構圖合併就把兩支程式碼合併成一支**——只有等 Decoder 真正做出來，且 belief token 設計涵蓋兩者的判斷目標，才有可能真正在程式碼層級整合。 | 使用者與王崇穎討論確認 + 架構圖實際修改 |

| 44 | 2026-08 | **王崇穎回覆 8-K（#41 提出的缺口）處理方式，兩個技術選項**。針對「fetch_filings.py 只抓 10-K/10-Q、抓不到 8-K 即時揭露」這個缺口，王崇穎轉述張教授的建議，有兩種處理方式：(1) **覆蓋過去**——8-K 本身也算一次事件，發生時直接取代目前「沿用上一份 10-K/10-Q 到下一份發布為止」邏輯裡被沿用的舊內容，讓當天的 filing_chunks 改成以最新的 8-K 為準；(2) **用 Text encoder 做 fusion**——新舊內容都保留，一起送進文字編碼器，並在前面加上 Prefix Prompt，讓模型自己判斷時間順序或看到時間戳記，藉此區分「已經持續一陣子的背景資訊」跟「剛發生的新事件」。**目前狀態**：這是技術做法的選項說明，不是「要不要抓 8-K」這個問題本身的答案——是否要新增 8-K 抓取（改 `fetch_filings.py` 的 `FORMS` 清單）仍待團隊評估是否值得投入；若之後決定要做，這兩個選項是已經確認可用的實作方向，選項 (2) 更貼近計畫書 3.2 節文字編碼器融合多來源文字的精神，選項 (1) 實作成本較低、改動範圍小。 | 王崇穎轉述張教授回覆 |

| 45 | 2026-08 | **實作 8-K 抓取，採 `#44` 兩個選項裡的第二個（Text encoder fusion + 時間戳記），不用覆蓋過去**。理由：讀了 `collect_filing_chunks()` 實際邏輯後發現「覆蓋過去」風險比預期具體——現有邏輯是硬性 `max(valid, key=filing_date)`，只留最新一份、其餘全部捨棄；8-K 內容通常很短（單一事件），若跟 10-K/10-Q 混在同一套「取最新一份」邏輯裡，會導致往後最長一整季（到下一份 10-K/10-Q 為止）RAG 只看得到單薄的 8-K 內容，完整財報的風險因素、MD&A 等豐富背景全部消失，這是實質的資料品質倒退，不只是重演 `#32` 的舊資料誤判問題。**改法**：`fetch_filings.py` 的 `FORMS` 加入 `"8-K"`；`build_dataset.py` 的 `collect_filing_chunks()` 拆成兩條邏輯——10-K/10-Q 維持原本「取最新一份沿用到下一份發布為止」當背景，不變；8-K 另外列成獨立的補充事件 chunk，每則標上「N 天前的重大訊息揭露 8-K」前綴（比照新聞 `days_ago` 手法，`#21`），只列「比目前背景這份 10-K/10-Q 還新」的 8-K，一旦有新一份 10-K/10-Q 發布、背景更新，舊的 8-K 自動被排除，不需要額外規則判斷「還算不算新事件」，也不會無上限地累積。**驗證**：沙盒內用假資料建構三個情境測試（背景期間內只有 1 則 8-K／有 2 則 8-K／背景換新後舊 8-K 自動消失），三種情境輸出的 chunk 列表、天數標記、背景更新後的自動排除行為皆符合預期；`py_compile` 確認語法正確。尚未用使用者本機的真實 SEC 資料實測（需要重跑 `fetch_filings.py` 抓 8-K、重跑 `build_dataset.py`），下次使用者本機執行時可以順便驗證。`event_extraction.py` 的既有日期過濾邏輯（`filing_date == 當天` 才算新事件）不受影響，8-K chunk 只會在真正發生的那天被算進去，其餘天數自動視為背景，不用改動。 | 使用者評估兩個選項後決定採用方案 (2) + 實作 |

| 46 | 2026-08 | **實作 ViT domain gap 對照實驗（H_v 有/無），沿用 `#28` 新聞歸零實驗同一套手法；使用者本機實測完成，K 線圖對分類準確度的貢獻遠大於預期**。`fusion/train.py` 新增 `--ablate_vision`（讀進 H_v 後在記憶體歸零，不改動磁碟向量檔案，做法跟既有 `--ablate_news` 完全對稱）。**實測結果（AAPL、`--weighted`、其餘設定不變）**：baseline（K 線圖照常）accuracy=0.3269、macro f1=0.3244；拿掉 K 線圖（`--ablate_vision`）accuracy=0.3125、macro f1=0.1587。accuracy 只掉 4.4%（0.3269→0.3125），**macro f1 掉了 51%**（0.3244→0.1587，絕對值掉 0.1657）——跟 `#28` 新聞歸零實驗同一個模式：accuracy 因為測試集三類已經接近平衡（`#30` 分位數門檻），不太會反映類別坍縮，macro f1 才是看得出真相的指標。拿掉 K 線圖後的 macro f1（0.159）甚至低於 `#28` 修正類別加權**之前**的坍縮基準線（0.247），代表現在的視覺輸入（K 線圖經過現成、未做領域訓練的 ViT）**是目前 Z_fused 裡貢獻最大的單一模態**，拿掉造成的傷害比任何一輪先前的方法論修正（類別加權、標籤定義改分位數）帶來的進步幅度都大。**對 domain gap 疑慮（`#10`/`#36`）的意義**：這不代表 domain gap 不存在或不重要——計畫書 3.1 節仍然明確要求財經圖表語料預訓練的視覺編碼器，現在用的通用 ImageNet 版本仍是落差。但這筆證據說明方向是反過來的：現在的 ViT 不是「沒用、可以先不管」，是「已經很有用、換成領域預訓練版本可能有更大的邊際效益」，不是「現在這個部分是拖累、要盡快換掉」。**誠實記錄限制**：只跑一次（3 epochs、單一 seed），不是多次重複取平均，訓練資料量本來就不大（1381 天）、模型也小，單次結果會有雜訊；但這次差距（0.166 絕對值）遠大於過去任何一輪對照實驗量到的差異（`#28`、`#30` 都是 0.02~0.06 量級的變動），量級差一個數量級，不太可能純粹是雜訊，可以當作可信的初步訊號，但嚴謹起見之後有餘力可以多跑幾個 seed 取平均驗證穩定性。**實驗後已還原**：使用者跑完立刻重跑一次不加 `--ablate_vision` 的正常版本，`data/outputs/z_fused/AAPL/` 已還原成正式版本，PPO/回測不受影響。 | 使用者本機執行 + 實測結果 |

| 47 | 2026-08 | **8-K 實作（`#45`）本機真實 SEC 資料驗證通過**。使用者本機重跑 `fetch_filings.py --ticker AAPL --start 2021-01-01`，71 筆 filings（含 48 筆 8-K）下載成功；重跑 `build_dataset.py` 重建 1381 筆每日 JSON。第一次驗證挑的日期（2026-08-01）沒看到 8-K，排查後確認是正常行為——當天最新背景已經是 2026-07-31 的 10-Q，前一天（2026-07-30）的 8-K 比背景還舊，依設計自動排除，不是 bug。改挑 2026-04-25（介於兩份 10-Q 之間、期間有兩則 8-K）重測：`filing_chunks` 正確同時出現 1 筆 10-Q（2026-01-30，當背景，內容完整未被覆蓋）+ 2 筆 8-K（2026-02-24 標「60 天前」、2026-04-20 標「5 天前」），三者並存，跟設計完全一致。至此 8-K 抓取＋不覆蓋背景＋時間戳記三個目標，從沙盒假資料測試到使用者本機真實資料測試都驗證通過，`#41` 提出的缺口正式補上。 | 使用者本機執行 + 實測驗證通過 |

| 48 | 2026-08 | **推翻 `#29` 的「待老師確認優先順序」前提：decoder、Integrated Gradients、curriculum learning、多資產回測，改為不用等老師表態，由使用者自行按架構判斷順序直接動工**。使用者明確裁示：問負責人／教授的時機是「遇到問題」或「確認目前進度有沒有要改善」，不是每個待做項目都要先問過優先順序才能開始；`#29` 當初「先記錄、暫不動工」的保留態度，在這四項上不再適用。**決定動工順序（按工程成本與相依性判斷，非老師指定）**：(1) Integrated Gradients——不需要新資料、直接套在已訓練好的 PPO policy 上，範圍最小、最獨立；(2) curriculum learning——不需要新資料，只需重排 `train_ppo.py` 的訓練排程；(3) 多資產回測——需要先擴充第二支股票的完整 A/B/C 資料才有意義測試，資料成本高於程式碼成本；(4) 生成式 decoder——範圍最大、最不確定，需要 A 端先產出全新的結構化敘述標準答案（y_belief），且需要真正的生成式模型（LLaVA/LLaMA-2 量級），建議之後另外開一輪 grill-me 仔細規劃再動工，不適合直接開始寫。 | 使用者明確裁示，不用等老師優先順序 |

| 49 | 2026-08 | **實作 Integrated Gradients（`#48` 排序第一項，計畫書 3.6 節 (4) 公式）**。新增 `module_c_fusion/explainability/integrated_gradients.py`：讀 `data/outputs/checkpoints/ppo_agent.zip` 訓練好的 PPO agent，把 policy 的動作分布 mean（確定性動作，可微分；抽樣動作不平滑不能算梯度）包成 `captum.attr.IntegratedGradients` 看得懂的函式，baseline 用零向量（計畫書原文建議），逐日對 observation（Z_fused 768 維 + 前期持倉 1 維）算歸因值。**前期持倉的處理**：不是隨便填零，而是先用訓練好的 agent 依序跑過整個序列重建出跟 `backtest.py` 的 `weights_ppo()` 實際運作一致的持倉軌跡（`rebuild_holding_trajectory()`），確保歸因分析對應的是 agent 真實會遇到的狀態，不是脫節的假設情境。輸出 `data/outputs/explainability/{ticker}/attributions.npy`（逐日逐維歸因）+ `summary.json`（各維度平均絕對歸因排序、前 10/後 10 名、前期持倉維度的歸因值，方便跟 Z_fused 本身對照）。`requirements.txt` 加入 `captum`。**驗證範圍**：沙盒沒有 torch/stable_baselines3/captum（兩次安裝嘗試都失敗，見本檔案更早的環境限制記錄），只能驗證不依賴這三者的部分——`py_compile` 語法檢查通過；`summarize()` 這個純 numpy 邏輯函式用假造的歸因矩陣做了單元測試（刻意放大兩個維度的絕對值，確認排序、正負號取絕對值、前後 10 名邏輯都正確）。`load_agent()`／`compute_attributions()`（真正呼叫 captum 算梯度積分的部分）**尚未在任何環境跑過**，需要使用者本機執行 `python -m module_c_fusion.explainability.integrated_gradients --ticker AAPL` 驗證：包含先 `pip install captum`、確認能正確載入 PPO agent、跑出合理的歸因數字（不是全零或 NaN）。 | 依 `#48` 決定的順序自行動工，尚待使用者本機驗證執行結果 |

| 50 | 2026-08 | **查證發現：目前磁碟上的 Z_fused 相關驗證報告全部是舊的、還沒吃到 8-K 資料，`classification_report.json` 甚至是消融實驗的殘留結果**。使用者問「分類驗證＋事件驗證頭＋decoder 目前進度」，重新比對檔案時間戳與程式碼依賴關係，發現兩層問題。**(1) `classification_report.json` 是 `#46` 消融實驗的殘留**：內容 accuracy=0.3125、macro f1=0.159，跟 `#46` 記錄的「拿掉 K 線圖」數字完全一致，不是還原後的正常版本；檔案 mtime（08-16 20:26）早於 `data/outputs/z_fused/AAPL/` 的 `.npy` 最後還原時間（08-16 20:32），代表使用者還原 Z_fused 後沒有重新跑 `classifier.py`。**(2) 更根本：8-K 還沒有真正流入 Z_fused**。目前 Z_fused（08-16 20:32 產生）用的 B 向量（`data/vectors/AAPL/*/H_r.npy` 等）是 08-10 產生的舊版；而 8-K 的 `build_dataset.py` 重跑（讓每日 JSON 的 `filing_chunks` 真正含 8-K，見 `#45`/`#47`）是 08-16 20:42~20:51 才做的——順序是「先產生 Z_fused，後改 daily JSON」。追蹤 `generate_vectors.py` 第 86 行 `db.add_chunks(record["filing_chunks"] + ..., text)` 確認 H_r 的 RAG 語料庫直接讀 `filing_chunks`，代表 B 端（`generate_vectors.py`）從沒有在 8-K 資料重建之後重新跑過。**結論**：8-K 只在 `build_dataset.py` 輸出層級驗證通過（`#45`、`#47`），完全還沒流入 H_r/Z_fused；現有的 `classifier.py`、`event_validation_head.py`（報告 08-11 產生，更早）、已訓練的 PPO agent、以及 `#49` 剛寫好的 Integrated Gradients，全部都是建立在「無 8-K」的舊 Z_fused 上，不是最新狀態的反映。**需要的重跑順序**：`module_b_encoder.generate_vectors --ticker AAPL` → `module_c_fusion.fusion.train --ticker AAPL` → `classifier.py`／`event_validation_head.py`（可選）→ 重訓 PPO agent（若要讓 IG 歸因也反映 8-K）。 | 使用者詢問目前進度，重新查證檔案時間戳與程式碼依賴關係後發現 |

| 51 | 2026-08 | **`#50` 建議的重跑順序，使用者本機執行完成，8-K 正式流入 Z_fused，但診斷分類指標幾乎沒有變化**。依序重跑 `generate_vectors.py --ticker AAPL`（B 向量，08-17 10:48）→ `fusion/train.py --ticker AAPL --weighted`（Z_fused，08-17 10:55）→ `classifier.py --weighted`／`event_validation_head.py`；時間戳確認 A（08-16 20:51）< B（08-17 10:48）< C（08-17 10:55），順序正確，8-K 這次真的流進 H_r/Z_fused了。**結果對比（AAPL、`--weighted`）**：市場情緒分類——8-K 前（`#46` baseline）accuracy=0.3269／macro f1=0.3244，8-K 後 accuracy=0.3221／macro f1=0.3193，差距在 0.005 量級，跟同一份 baseline 重跑一次的自然雜訊差不多大，**看不出有意義的變化**。事件驗證頭——8-K 前（`#34`/`#37`）micro f1=0.229，8-K 後 micro f1=0.231，同樣是雜訊量級的差異。**誠實解讀**：8-K 資料本身確認有進到 pipeline（不是白做），但對這兩個既有診斷指標沒有可觀察的影響，合理的原因是 8-K 事件本來就稀疏（48 筆 8-K 分散在 1381 天，約 3.5% 的天數），而且這兩個診斷任務（市場方向 3 類、既有 7 類商業事件）跟「這天有沒有 8-K 重大訊息」不是直接對應的驗證目標，8-K 的效果如果存在，比較可能要用專門針對「8-K 發生日 vs 沒有 8-K 的日子」做對照才看得出來，不是目前這兩個既有指標能捕捉到的。**不代表 8-K 這個功能白做**：`#45` 的架構理由（避免舊 filing 內容被覆蓋、8-K 事件保留時間戳）本身站得住腳，跟診斷指標有沒有變化是兩件事。 | 使用者本機執行 + 實測結果 |

| 52 | 2026-08 | **實作 curriculum learning（`#48` 排序第二項，計畫書 3.6 節 anticipated challenge (2)）**。PDF 原文：「We will implement a curriculum learning strategy, starting the RL agent in simplified market environments (e.g., low volatility periods) and gradually introducing complex scenarios (e.g., market crashes) to improve robustness」，中文版工作項目也寫「導入課程學習以提升模型在極端行情下的穩健性」。`train_ppo.py` 新增 `compute_trailing_vol()`（逐日估計截至當天為止的滾動波動度，只看過去，不看未來）、`curriculum_stages()`（依波動度排名把資料切成 N 個由簡入繁的累積階段——階段 1 只挑全部資料裡最平穩的一批，逐步放寬，最後一階段強制等於完整資料集，含最劇烈的高波動/崩盤時期）。**篩選天數不影響 no-look-ahead**：每天的 next-day return 是 A/C 事先算好、跟著那天走的固定值，篩選只是決定「這階段餵哪些天」，不是在 env 裡臨時往未來看；階段內部維持原本時間序（不是按波動度重新排序），因為 env 的前期持倉狀態要延續「照時間往前走」的意義。新增 `--curriculum`（預設關閉，行為與加入之前完全一樣，方便直接跟不開的版本做 A/B 對照，沿用 `--ablate_news`/`--ablate_vision` 同一套「診斷開關預設關閉」慣例）與 `--curriculum_stages`（預設 3 階段）。開啟時用 `model.set_env()` + `reset_num_timesteps=False` 讓同一個 PPO policy 依序在三個階段上連續訓練（不是每階段重新初始化），對應「漸進式導入複雜情境」的設計意圖。**驗證**：`py_compile` 通過；沙盒可以直接測 `compute_trailing_vol`/`curriculum_stages` 這兩個純 numpy 函式（train_ppo.py 的 `from stable_baselines3 import PPO` 延遲到 `main()` 內才 import，模組層級沒有 torch 依賴），用「前半段低波動、後半段高波動」的合成資料測試：階段天數嚴格遞增（100/200/300）、最後階段完全等於原始資料、每階段內部保持嚴格遞增的時間序索引（沒有被打亂）、階段 1 100% 落在低波動的前半段——全部符合預期。真正呼叫 PPO 訓練的部分（`model.learn()`/`model.set_env()`）沒有在任何環境跑過，需要使用者本機執行 `python -m module_c_fusion.rl.train_ppo --ticker AAPL --curriculum` 驗證，並與不開 `--curriculum` 的版本（重訓一次）比較 `backtest.py` 的 Sharpe/MDD，才知道 curriculum learning 對這個專案的資料量（1381 天，尚不算長）有沒有實質幫助。 | 依 `#48` 決定的順序自行動工，尚待使用者本機驗證執行結果 |

| 53 | 2026-08 | **`#52` 本機實測：baseline PPO 表現不錯，但 `--curriculum` 版本訓練出一個「永遠空手」的退化策略（policy collapse）**。使用者依序跑完 baseline（不開 curriculum）→ backtest → curriculum 版本 → backtest → Integrated Gradients。**Baseline 結果（用現在含 8-K 的 Z_fused，`--timesteps` 預設 10000）**：cumulative_return=8.03%、Sharpe=0.67、MDD=2.9%——比 buy_and_hold（Sharpe=0.66、MDD=33%，見更早的 `report_20260727_232105`）風險調整後表現更好、回撤小非常多，是目前為止最像樣的 PPO 結果。**Curriculum 結果**：cumulative_return=0%、Sharpe=0、MDD=0%、final_nav 剛好=1.0——回測報告數字精確全部是 0，代表這個 agent 對全部 1381 天都輸出「持股權重 0」，從頭到尾沒有真正進場過一次，不是表現差，是完全沒有交易行為。**根因推測（尚未實機驗證，只能靠讀程式碼＋數字反推）**：`--curriculum` 預設把 `--timesteps`（10000）平分成 3 個階段，每階段約 3333 steps；PPO（stable-baselines3）預設 `n_steps=2048`，代表每個 rollout buffer要收滿 2048 steps 才會做一次策略更新，3333 steps 大概只夠做 1 次、最多 1 次多一點的更新，比 baseline 一次性用滿 10000 steps（約 4-5 次更新）少了很多；風險敏感的 reward（`env.py`：`reward = r_portfolio − λ_vol·vol − λ_mdd·drawdown`）在更新次數不足、policy 還沒學會怎麼抓報酬訊號時，容易讓 agent 收斂到「永遠不持倉」這個局部最優（不持倉保證 reward 不會因為波動/回撤被扣分，是最安全但沒有意義的策略），這是 RL 用在投資組合任務上常見的已知病態（不是這次 curriculum 實作獨有的新問題，只是這次用比較少的 timesteps 更容易踩到）。**IG 結果同樣受影響**：`integrated_gradients.py` 是對這次（退化的）curriculum agent 跑的，`summary.json` 的歸因數值都很小（最大 0.003、平均 0.0003），跟 policy 輸出幾乎是常數（每天都約等於同一個會被裁成 0 的負值）一致——歸因不是壞掉，是忠實反映了這個 agent 本身沒什麼可解釋的行為，換句話說目前這份 IG 結果不能拿來當作正式的可解釋性分析依據，要等 curriculum 訓出一個真正在動的 policy 之後重跑。**下一步建議**：`--curriculum` 搭配更大的 `--timesteps`（例如 30000，讓每階段仍有接近 baseline 的 10000 steps 預算）重跑一次，看能不能訓出跟 baseline 同等級、但同時具備「漸進式導入複雜情境」設計意圖的 policy；如果還是收斂到空手，代表問題不只是 timesteps 不夠，需要進一步看 reward 設計或 PPO 超參數（例如提高 `ent_coef` 增加探索）。 | 使用者本機執行 + 實測結果，退化原因待進一步驗證 |

| 54 | 2026-08 | **`--timesteps 30000` 重跑仍然退化成永遠空手，排除「單純步數不夠」這個假說；改成階段間不均分步數 + 開放 `ent_coef` 診斷開關**。使用者依 `#53` 建議把 `--timesteps` 從 10000 加到 30000 重跑 `--curriculum`，backtest 結果（`report_20260817_150144.json`）仍然是 cumulative_return=0／Sharpe=0／MDD=0，跟第一次一模一樣，證明「均分後每階段步數不夠」不是（唯一）根因。**修正後的假說**：三階段均分步數時，最簡單（低波動子集）的階段 1 反而有充足步數快速收斂——低波動天數本身報酬訊號小，risk-sensitive reward 底下「永遠不持倉」在這個子集上很可能本來就接近局部最優（不持倉保證不會被 vol/drawdown 懲罰，且交易成本也不用付），一旦 policy 在階段 1 收斂到這個狀態、action 分布的標準差跟著收斂變小，PPO 的 clip 機制會讓後續階段的更新很難把它拉回來，不管後面階段給多少步數都一樣——這解釋了為什麼單純加大 `--timesteps` 沒用（加大的步數大部分被浪費在「鞏固」已經收斂的階段 1，不是拿去矯正它）。**兩個對應的程式碼修正**：(1) 新增 `curriculum_stage_timesteps()`，把步數改成依難度線性遞增分配（權重 1,2,...,n_stages），讓最後（完整資料）階段拿到的步數不少於原本一次性訓練的量，前面階段步數壓低、減少收斂到局部最優的機會與時間（`--timesteps 10000, 3 階段`：舊版 [3333,3333,3334] → 新版 [1666,3333,5001]；單元測試確認總和不變、嚴格遞增）。(2) 新增 `--ent_coef`（預設 0.0，跟 stable-baselines3 原本預設值一樣，不動 baseline 行為），若權重調整還是不夠，可以試著調大（如 0.01）鼓勵探索、避免 action 標準差太快收斂到接近 0。另外在 curriculum 訓練結束後加一行診斷輸出 `model.policy.log_std`，數字越負代表分布標準差越小、探索越少，可以直接從訓練輸出判斷有沒有踩到「標準差收斂」這個機制,不用等 backtest 結果才知道。**驗證**：`py_compile` 通過；`curriculum_stage_timesteps()` 純數字邏輯已單元測試（多組 total/n_stages 組合，確認總和守恆、嚴格遞增）。真正重訓的結果尚待使用者本機執行 `--curriculum --timesteps 30000` 驗證新版本是否解決退化問題。 | 使用者本機執行 + 實測結果，排除步數不足假說，改用階段加權步數 + log_std 診斷 |

| 55 | 2026-08 | **`#54` 的修正確認有效：curriculum learning 訓出正常運作的 policy，跟 baseline 表現相當**。使用者本機重跑 `--curriculum --timesteps 30000`（階段加權步數版本），訓練輸出印出 `log_std=-0.0963`（std≈0.91，全程三階段的 std 數字都維持在 0.9~0.99 區間，沒有收斂到接近 0），確認 `#54` 診斷成功——標準差沒有崩塌，agent 有正常維持探索。backtest 結果：cum=8.90%、Sharpe=0.67、MDD=3.24%，對比 baseline（`#53`：cum=8.03%、Sharpe=0.67、MDD=2.9%），兩者 Sharpe 完全打平，curriculum 版本報酬略高、回撤也略高一點點，量級上是同一水準的正常策略，不是退化版本。**結論**：curriculum learning 現在是一個可用、跟 baseline 打平的訓練方式，額外符合計畫書 3.6 節 anticipated challenge (2) 明訂的「先簡化情境、逐步導入複雜情境」設計意圖；這次資料量（1381 天）跟這個 reward 設計下，看不出 curriculum learning 比一次性訓練有明顯優勢，但也沒有變差，且訓練機制本身（階段加權步數）已證明正確可用，之後資料量變大或換更長的历史區間時仍然是有效的工具。 | 使用者本機執行 + 實測結果，退化問題確認解決 |

| 56 | 2026-08 | **Integrated Gradients（`#49`）在 `#55` 修好的健康 policy 上重跑，確認是有意義的歸因結果，`#48` 排序的第一、二項（IG、curriculum learning）正式收尾**。使用者重跑 `python -m module_c_fusion.explainability.integrated_gradients --ticker AAPL`，console 印出 Z_fused 平均絕對歸因=0.000197、最高貢獻維度 dim=50。額外查證完整 `summary.json` 與 `attributions.npy` 統計，確認這次不是像 `#53` 那樣的「近似常數輸出」：top10 維度歸因值落在 0.001~0.0017，bottom10 落在 5e-7~7e-8，相差約 4 個數量級，明確的維度間差異化；768 維裡有 373 維（48.6%）歸因 > 1e-4（有意義的量級），只有 1.8% 的維度 < 1e-6（幾乎沒貢獻），不是「每一維都差不多、看不出重點」的扁平分佈；前期持倉這一維的歸因（0.000265）落在 Z_fused 各維度的中高段，量級合理，不是被忽略也不是不成比例地主導。**結論**：這份 IG 結果可以正式當作可解釋性分析的依據使用，Z_fused 裡確實有一小群維度（約一半，373/768）在驅動 PPO 的持股決策，另一半貢獻很小，這個「集中在部分維度」的模式本身也是一個值得寫進論文的觀察（可以進一步去看那些高貢獻維度是不是集中對應某個模態，但目前 Z_fused 是三個模態融合後的單一向量，沒有天生的維度-模態對應關係，要做這層分析需要額外設計，非本次範圍）。至此 `#48` 排序的四項裡，Integrated Gradients、curriculum learning 兩項都已完成並實測驗證，只剩多資產回測、生成式 decoder 兩項待後續處理。 | 使用者本機執行 + 完整統計查證 |

## 待確認事項（追蹤用）

| 項目 | 何時需要答案 | 狀態 |
|---|---|---|
| 事件抽取的 ground truth 來源與事件類別定義 | B 開始做事件抽取驗證前 | 已解決（見 #32）：150 天分層抽樣，LLM 輔助標記（這一輪用 Claude 對話標記，之後可切換 API 腳本），七類事件維持現有定義，之後如需擴大範圍可用 `event_ground_truth_llm.py` |
| 「市場情緒分類」的正式定義 | C 寫分類驗證前 | 已解決（見 #24、#38）：教授確認未來 5 日漲跌標籤方向正確，之後可考慮加入成交量、交易頻率、事件觸發等輔助訊號讓標籤更可靠，但先以現有做法當 baseline，不用立刻擴充 |
| 新聞時間切齊規則（盤後新聞歸屬） | A 設計新聞爬蟲時 | 已確認（見 #23），implementation 待補上 |
| train/val/test 的時間切分方式 | C 正式訓練前 | 未定，但目前實作已採時間序切分（見 classifier.py），比例本身影響不大 |
| yfinance 是否為合適股價來源 | A 開始正式抓資料前 | 實質已由 #22（技術選型彈性）涵蓋，不需另外確認 |
| 指數/ETF 若延伸，需抓哪些總經資料 | 若後續真的擴充到指數/ETF | 已解決（見 #38）：教授確認改用總經資料（CPI、PCE 等），並建議聯準會利率點陣圖（圖表格式，可直接用現有 ViT 處理）；`fetch_macro.py` 骨架已建立，尚未實作，待真的要擴充到 ETF 時再補上 |
| 新聞歷史回補（2021~2023 缺口）改用什麼資料來源/方法 | A 繼續推進新聞回補前 | 已解決（見 #27）：改用 Alpaca News API，本機實測成功 |
| 生成式 decoder（正式計畫書 3.4 節，用 Z_fused 生成結構化市場敘述）要不要做 | 老師確認前 | 已知必要（見 #29），暫不動工；需 A 端另外產出結構化敘述標準答案 |
| 跨模態可解釋性 Integrated Gradients（正式計畫書 3.6 節，套在 PPO policy 上）要不要做 | 已完成，見 #49、#56 | 已解決：`integrated_gradients.py`（captum + 零向量 baseline），在健康的 policy 上重跑確認歸因結果有意義（維度間差異化明確，373/768 維有實質貢獻），見 #56 |
| `event_extraction.py`（讀 A 原始資料判斷事件，不碰 Z_fused）的定位跟論文寫法 | 寫論文方法論章節前 | 已解決（見 #35、#37）：計畫書沒有這個項目的依據，只能定位成 Track A 資料集品質驗證的附屬分析（不是系統模組、不是驗證 Z_fused）；149 天測試 f1=0.667（LLM 版，見 #35）已足夠支撐這個定位，全量 1381 天結果對這個定位沒有加分，不需要再投入 |
| Z_fused 的事件驗證頭（計畫書第 19 頁明訂的「診斷性分類測試」工作項目） | 已完成，見 #34、#37 | 已實作且已確認計畫書依據（見 #34、#37）：`event_validation_head.py`，micro f1=0.229（可評估的 5 類），計畫書第 19 頁「表徵效能驗證」直接要求這件事，不是團隊自己額外加的設計，正當性明確；樣本數小、屬初步結果 |
| ViT 與 K 線圖的 domain gap 比較實驗（#10 決議要求） | 已執行，見 #46 | 落差本身仍未解決（計畫書 3.1 節要求財經圖表語料預訓練版本，現用通用 ImageNet 版本不變），但已有實測證據（#46）：拿掉 K 線圖 macro f1 掉 51%（0.324→0.159），現有 ViT 貢獻很大，不是「沒用」，換編碼器的迫切性下降，但邊際效益可能仍大，留待老師確認優先順序時一併參考這筆證據 |
| RL 訓練的 curriculum learning（正式計畫書列為訓練穩定性風險應對方案） | 已完成，見 #52～#55 | 已解決：`--curriculum` 開關（波動度分階段 + 階段加權步數 + log_std 診斷），本機驗證確認訓出正常 policy，跟 baseline 打平（Sharpe 都是 0.67），見 #55 |
| 回測/RL 擴充到多資產（多支股票）投資組合配置 | 若要擴充股票池前 | 未解決（見 #29），`backtest.py` 目前僅支援單一股票持有/空手配置 |
| ~~三版本 Z_fused 消融實驗（H_e：無/關鍵字/LLM 事件抽取當第四輸入）要不要做、怎麼測~~ | 已解決（見 #42） | **確認不做**：原始動機是讓事件抽取跟系統核心產生關聯，這個關聯已經被既有的分類驗證（`classifier.py`）+ 事件驗證頭（`event_validation_head.py`）滿足，負責人確認「先不用做」，(1)(2)(3) 三個疑慮不再需要解決 |
| Technical Indicators 是否要獨立做視覺化輸入（Figure 1 架構圖暗示，正文未明講） | 若要擴充 A 的圖表產出前 | 未解決（見 #40）：只是圖示細節，程度較輕，非緊急，待團隊評估是否要做 |
| 財報是否要新增 8-K（重大訊息即時揭露）抓取，補上目前 10-K/10-Q 抓不到的即時事件 | 已完成，見 #45、#47、#50、#51 | 已解決（見 #41、#44、#45、#47）：採張教授建議的方案 (2)（Text encoder fusion + 時間戳記），`fetch_filings.py`/`build_dataset.py` 已改完，沙盒假資料測試 + 使用者本機真實 SEC 資料（71 筆 filings，含 48 筆 8-K）皆驗證通過；`#50` 發現 8-K 一度沒有真正流入 Z_fused（B/C 沒有在資料重建後重跑），`#51` 補跑完整 pipeline 後確認已流入，但市場情緒分類／事件驗證頭兩個既有診斷指標變化在雜訊量級，看不出明顯影響，不代表功能本身有問題 |
| 「事件」的定義是否該涵蓋 K 線圖技術型態（頭肩頂、W 底反轉等），不只是現有的七類商業事件 | 若要擴充事件驗證頭的驗證範圍前 | 未解決：與王崇穎、張教授討論「Z_fused 診斷性分類測試」時，教授舉的「關鍵技術事件」例子是頭肩頂、W 底反轉型態（K 線圖視覺型態辨識），跟 `event_validation_head.py` 現有測的七類商業事件（EARNINGS/MA/PRODUCT_LAUNCH/LAWSUIT/GUIDANCE/DIVIDEND/MANAGEMENT_CHANGE，來自新聞/財報文字）性質不同，目前完全沒有對應的 ground truth 或驗證機制。是否要另外處理，待團隊評估，非緊急 |
