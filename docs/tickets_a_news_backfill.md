# Tickets：A 新聞資料歷史回補

對應規格：`docs/spec_a_news_backfill.md`（2026-08 grill-me → to-spec → to-tickets 產出）

**狀態（2026-08）：已結案，FNSPID 路徑不採用。** 票 1 實測發現 nasdaq.com 對自動化瀏覽器做偵測阻擋、且頁面前端已改版導致 scraper 選取器失效；現成 HuggingFace dataset 重新驗證後 `Article` 全文欄位確認為空。詳見 `docs/spec_a_news_backfill.md`「驗證結果」與 `docs/decisions.md` #26。票 2 因此取消，票 4 的整合驗證無標的可整合、一併結案。

**後續（2026-08）**：改用 Alpaca News API，本機實測成功（AAPL 2021~2026，15,989 則、1,459 天、約 10 分鐘），見 `docs/decisions.md` #27、`module_a_data/crawler/fetch_news_alpaca.py`。本文件內提到的 `FNSPID/`、`fetch_news_fnspid.py`、`fetch_news_local_scrape.py` 皆已實際刪除，以下票的內容保留作決策過程紀錄。

工作前緣（frontier）：任何「被誰卡住」都已完成的票，就是目前可以動工的票。票 1 和票 3 一開始就都可以動工。

## 票 1：環境準備 + Headline Scraper 範圍測試（AAPL）

**要做的事**：確認能在本機跑起爬蟲，並且針對 AAPL 實際測出新聞標題能回溯到多久以前，回答「5 年範圍夠不夠」這個問題。

**被誰卡住**：沒有 — 可以馬上開始

- [x] selenium 已加進 requirements.txt，sandbox 內驗證可以正常 import（實際執行需要真的瀏覽器，只能在有網路權限的本機做）
- [x] headline scraper（`find_headlines v0.2.py`）加上「翻到目標起始年份（2021-01-01）就停止翻頁」的邏輯，日期解析邏輯已測過（相對時間 + 絕對日期兩種格式都處理）
- [x] 針對 AAPL 實際執行 —— **已在本機執行，結果為失敗**：Selenium 卡在無限重試迴圈，追查為 nasdaq.com 對自動化瀏覽器做偵測、不提供真實新聞內容（換用 Claude in Chrome 測同一網址，結果一樣；手動瀏覽器開啟則正常），且頁面前端已改版，scraper 寫死的 class 名稱已不存在於現在的頁面上
- [x] 明確記錄結果 —— **回溯深度無法測得**（尚未到「抓不到足夠早的日期」這個層次的問題，是更前面一步「抓不到任何內容」）。結論：此路徑不可行，詳見 `docs/spec_a_news_backfill.md` 驗證結果段落

## 票 2：Content Scraper 執行（AAPL）—— **已取消**

**要做的事**：~~針對票 1 抓到的標題清單，執行內文抓取，取得新聞全文，接受目前已知偏低的成功率。~~

**被誰卡住**：票 1 —— 票 1 沒有產出可用的標題清單（headline scraper 本身就抓不到內容），此票無標的可執行，取消。

- [x] ~~針對票 1 產出的標題/連結清單執行內文抓取~~ 不適用，票 1 無產出
- [x] ~~產出實際新聞全文資料~~ 不適用
- [x] ~~記錄整體成功率與涵蓋的日期範圍~~ 不適用

## 票 3：新聞資料轉換腳本（爬蟲輸出 → 專案新聞 JSON 格式）

**要做的事**：寫一支腳本，把爬蟲自己的輸出格式，轉換成本專案既有的每日新聞 JSON 格式，可以先用現成的範例資料開發與驗證。

**被誰卡住**：沒有 — 可以用現有範例資料先開發測試，跟票 1 平行進行

- [x] 轉換腳本（`module_a_data/crawler/fetch_news_local_scrape.py`）能讀懂爬蟲輸出的欄位，正確對應到專案新聞 JSON 的欄位
- [x] 套用既有的盤後新聞歸屬規則（沿用 `fetch_news.py` 的 `_effective_date`），行為與現有新聞來源一致
- [x] 用現有範例資料測試：143 筆真的抓到全文的資料，141 筆成功轉換（2 筆日期格式無法解析而跳過），輸出格式跟現有新聞來源一致
- [x] 依日期正確分檔存放，已驗證輸出檔案（`data/raw/news/AAPL/2024-01-25.json` 等）

（這次測試用的是範例資料，只涵蓋 2024/01-02 少數幾天，不是完整 5 年回補，正式資料要等票 1、票 2 在本機跑完才有。）

## 票 4：整合驗證 —— **已結案（無標的可整合）**

**要做的事**：~~把票 2 真正抓到的 AAPL 資料，用票 3 的轉換腳本處理，確認能銜接進現有的資料建置流程~~；順便對舊的 FNSPID 轉換腳本做出去留的結論。

**被誰卡住**：票 2、票 3 —— 票 2 已取消，無真實資料可整合，前兩項不適用。

- [x] ~~用票 3 的轉換腳本處理票 2 的真實輸出~~ 不適用，票 2 無產出
- [x] ~~資料建置流程能成功讀到這批歷史新聞~~ 不適用
- [x] 對舊的 HuggingFace CSV 轉換腳本（`fetch_news_fnspid.py`）的去留做出明確結論 —— **結論：暫不刪除，但確定不會使用**。因為上游 dataset 全文欄位已驗證為空，這支腳本轉換出來的資料本來就不會有內容；先保留程式碼原地不動（作為之後若真的換到別的資料來源、需要類似欄位對應邏輯時的參考），但標記為不會被實際呼叫。`fetch_news_local_scrape.py`（票 3）本身邏輯沒有問題、程式碼正確，只是目前沒有真實資料可餵給它，一併保留、等新資料來源確定後再視格式決定是否直接沿用。
