# 統一開發環境（唯一依據）。安裝：pip install -r requirements.txt
# 版本先不鎖死，跑通後由團隊一次鎖定（pip freeze 後截取）。

# --- shared ---
numpy
pandas
pyyaml
tqdm
pillow              # PIL.Image，chart_generator.py 存圖、vision_encoder.py 讀圖都用到

# --- Module A: data engineering ---
yfinance
mplfinance
requests
beautifulsoup4
lxml
python-dotenv       # 讀 .env 裡的 Alpaca / Alpha Vantage API key，見 fetch_news_alpaca.py、fetch_transcripts.py

# --- Module B: encoders + RAG ---
torch
torchvision        # AutoImageProcessor（ViT 圖片前處理）需要，transformers 沒有把它列為必裝
transformers
sentencepiece
faiss-cpu          # GPU 機器可改 faiss-gpu；若改用 Milvus 則加 pymilvus

# --- Module C: fusion + RL + backtest ---
peft               # QLoRA
bitsandbytes
accelerate
gymnasium
stable-baselines3
matplotlib
scikit-learn
captum              # Integrated Gradients，見 module_c_fusion/explainability/integrated_gradients.py

# --- Module B: event_ground_truth_llm.py 建 ground truth、
# event_extraction.py --method llm 要打 LLM API 時才需要，見 docs/spec_b_event_extraction_llm.md ---
# openai 套件同時支援 --provider openai 跟 --provider deepseek（deepseek 是 OpenAI 相容
# 介面，換 base_url 就能用，目前實際在用的是這個）。用 --provider claude 才需要另外裝 anthropic。
openai
# anthropic
