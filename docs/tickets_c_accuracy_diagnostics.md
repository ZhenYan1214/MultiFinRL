# Tickets：分類準確率診斷（新聞訊號 vs. 類別不平衡）

對應規格：`docs/spec_c_accuracy_diagnostics.md`（2026-08 grill-me → to-spec → to-tickets 產出）

**狀態（2026-08）：全部完成。** 結論與下一步建議見 `docs/spec_c_accuracy_diagnostics.md`「驗證結果」、`docs/decisions.md` #28、`docs/data_and_experiments_log.md` 第三節。

工作前緣（frontier）：任何「被誰卡住」都已完成的票，就是目前可以動工的票。票 1 和票 2 一開始就都可以動工。

## 票 1：建立「沒有新聞」的向量版本（含備份）

**要做的事**：把目前已經算好的真實新聞向量完整備份一份；另外產生一份「新聞相關表徵歸零、圖表跟檢索表徵維持不動」的版本，讓後面的融合訓練/分類驗證可以改讀這份版本。

**被誰卡住**：沒有 — 可以馬上開始

- [x] ~~已有的真實新聞向量有一份完整備份~~ **改用更好的做法，不需要備份**：實測發現在雲端同步資料夾對上千個小檔案做複製非常慢（2 分鐘只處理 149/1230 天，不可行），改成在融合訓練程式裡加一個記憶體內開關，讀進新聞表徵後直接歸零、不寫回磁碟，從頭到尾沒有動過任何實際向量檔案，也就不需要備份
- [x] 產生「新聞被歸零、其餘不動」的版本，可以透過切換讓後面的步驟改讀這份版本 —— `fusion/train.py` 新增 `--ablate_news` 開關
- [x] 抽查幾天的資料，確認被歸零的只有新聞相關的表徵，圖表與檢索相關的表徵維持跟真實版本完全一致 —— 用 numpy 直接驗證：H_t 歸零後總和為 0，H_v/H_r 總和跟原始檔案一致

## 票 2：在訓練與分類驗證流程中加入類別加權支援

**要做的事**：融合層的表徵學習訓練步驟、以及最終的分類驗證步驟，都加上「是否依類別出現頻率做加權」這個可開關的選項。

**被誰卡住**：沒有 — 可以跟票 1 平行進行

- [x] 融合層訓練步驟可以選擇是否加權，預設關閉，不影響既有結果重現 —— `fusion/train.py` 新增 `--weighted` 開關（`CrossEntropyLoss` 依訓練集類別頻率反比加權）
- [x] 分類驗證步驟同樣可以選擇是否加權，預設關閉 —— `classifier.py` 新增 `--weighted` 開關（`LogisticRegression(class_weight="balanced")`）
- [x] 打開開關後，可以確認權重確實套用到訓練目標上 —— 加權後的四組結果跟不加權結果有明顯、一致方向的差異（BEARISH/BULLISH 表現從塌縮狀態動起來），確認權重有生效，不是加了選項沒作用

## 票 3：跑四種組合並蒐集結果

**要做的事**：新聞有無 × 加權有無，四種組合的分類結果都要有，各自標示清楚。

**被誰卡住**：票 1、票 2（兩者都要完成才能跑齊四種組合）

- [x] 「有新聞 + 不加權」沿用目前已有的結果，不用重跑 —— `data/outputs/metrics/classification_report.json`（accuracy=0.4649）
- [x] 「沒新聞 + 不加權」「有新聞 + 加權」「沒新聞 + 加權」三種組合各自完整跑過融合訓練與分類驗證
- [x] 四份結果都清楚標示對應哪一種組合，彼此不會混淆 —— 分別存成 `classification_report_noNews_noWeight.json`、`classification_report_news_weighted.json`、`classification_report_noNews_weighted.json`

## 票 4：把真實新聞的向量還原回正常狀態

**要做的事**：向量資料回到只有真實新聞版本的狀態，確認還原沒有出錯。

**被誰卡住**：票 3

- [x] ~~向量資料回到只有真實新聞版本~~ **不適用（改良後的做法不需要）**：因為票 1 改成記憶體內開關、從未寫回磁碟，向量檔案自始至終都是真實新聞版本，沒有需要還原的殘留狀態
- [x] 重新跑一次分類驗證（不加任何開關），結果跟這一輪實驗開始前的既有基準一致，確認沒有意外改到預設行為 —— 重跑後 `accuracy=0.4648648648648649`，跟基準逐位數字相同，回歸測試通過

## 票 5：判讀四份結果並記錄下一步建議

**要做的事**：依 spec 訂的判讀標準逐一檢視四份結果，把結論記錄下來，給出明確的下一步建議。

**被誰卡住**：票 3（跟票 4 平行，不用等還原完成）

- [x] 依照「看少數類別表現有沒有真的動起來，不只看整體準確率」的判讀標準，逐一檢視四份結果
- [x] 把四份結果與判讀結論，補進既有的資料與實驗紀錄檔案 —— `docs/data_and_experiments_log.md` 第三節
- [x] 針對「新聞有沒有幫助」「類別加權有沒有幫助」「兩者有沒有交互作用」三個問題，各自給出明確結論 —— 見 `docs/spec_c_accuracy_diagnostics.md`「驗證結果」
- [x] 依結論對照 spec 列出的候選下一步，給出具體建議並說明理由 —— 類別加權採為預設做法；下一步延伸 AAPL 時間範圍到 2026-08
