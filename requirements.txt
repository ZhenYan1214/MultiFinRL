# 統一開發環境（唯一依據）。安裝：pip install -r requirements.txt
# 版本先不鎖死，跑通後由團隊一次鎖定（pip freeze 後截取）。

# --- shared ---
numpy
pandas
pyyaml
tqdm
pillow              # PIL.Image，chart_generator.py 存圖、vision_encoder.py 讀圖都用到

# --- Module A: data engineering ---
yfinance
mplfinance
requests
beautifulsoup4
lxml
edgartools          # 財報下載候選（decisions.md #25），見 fetch_filings_edgartools.py
python-dotenv       # 讀 .env 裡的 Alpaca API key/secret，見 fetch_news_alpaca.py
boto3               # foolcalls 內部 import 需要（即使我們不用它的 S3 功能）
# foolcalls 不是正式上架的套件，PyPI 上沒有、GitHub 上也沒有 setup.py，
# 不能直接寫在這裡用 pip 裝。用法見 fetch_transcripts.py 檔頭的說明。

# --- Module B: encoders + RAG ---
torch
torchvision        # AutoImageProcessor（ViT 圖片前處理）需要，transformers 沒有把它列為必裝
transformers
sentencepiece
faiss-cpu          # GPU 機器可改 faiss-gpu；若改用 Milvus 則加 pymilvus

# --- Module C: fusion + RL + backtest ---
peft               # QLoRA
bitsandbytes
accelerate
gymnasium
stable-baselines3
matplotlib
scikit-learn
